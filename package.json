{
  "name": "expo-horizon-sdk",
  "private": true,
  "workspaces": [
    "expo-horizon-core",
    "expo-horizon-location",
    "expo-horizon-notifications"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "prepare": "yarn workspaces run prepare",
    "clean": "yarn workspaces run clean"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0"
  }
}
